// SETTINGS PRODUCTION
{
    "app": {
        // Port in which the server should run on production.
        "port": 8080,
        "url": "https://pandagainz.com/"
    },
    // Database disabled in production while features are not fully ready.
    "database": {
        "enabled": false,
        "name": "pandagainz"
    }
}
